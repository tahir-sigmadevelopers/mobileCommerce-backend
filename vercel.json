{
  "version": 2,
  "name": "ECOMMERCE-MERN",
  "builds": [{ "src": "public/app.js", "use": "@vercel/node" }],
  "routes": [{ "src": "/(.*)", "dest": "public/app.js" }]
}

// npm install && mkdir -p uploads && npm run build